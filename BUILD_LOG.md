website_vocab_cleanup | replaced 4 deprecated terms (Captain's Method, Four-Phase Engagement, AI agent, ops-risk-dashboard) across entire site; mechanical edits only, no copy or design changes; renamed files/components/slugs for ops-risk-dashboard → command-deck
brief_00_project_setup | Scaffolded Next.js + TS + Tailwind, dropped canon docs, configured color/type tokens, installed Inter Tight and JetBrains Mono.
brief_01_hero_section | Shipped hero: nav, veteran stamp, eyebrow, fragment-and-pivot hook, sub, risk banner with animated counter, Phase 3 dashboard with leaks + month sidebar + agent strip, primary and secondary CTAs. Responsive at 720px and 380px breakpoints. Judgment call: CTAs use 'use client' for 0.5px-bordered hover effects rather than CSS-only approach; avoids inline-style/Tailwind override complexity.
brief_02_insignia | Shipped Insignia component (full + simplified variants), BrandLockup composition (pin + wordmark + descriptor), favicon.svg, integrated simplified Insignia at 36px into hero nav replacing the placeholder mark. Judgment calls: (1) favicon includes the drop-shadow ellipses at base since they add depth at small sizes without noise; (2) 2026-05-14 decisions_log entries and design_canon Insignia section were absent from repo — added them as part of this brief before implementing.
brief_03_hero_rebuild | Migrated hero to locked customer-facing product naming (The X-Ray, The Command Deck, Watchdog, Handler). Added XRayGuarantee component between dashboard and CTAs. Replaced veteran stamp subtag (VETERAN-OWNED · PRACTICAL · PLAIN-SPOKEN). Updated agent strip to Watchdog language. Updated dashboard mockup labels (THE COMMAND DECK · SAMPLE, Command Deck — Acme Distribution). Replaced dual CTAs with single primary START A CONVERSATION. Judgment calls: (1) removed `uppercase` CSS class from the filename label in DashboardBar — "Command Deck — Acme Distribution" is title case and uppercase transform would render it wrong; (2) forbidden vocab grep flagged `textTransform` CSS property as false positive on "transform" — confirmed not copy.
brief_08.5_engagement_corrections | Three corrections to Section 6 (Engagement) shipped in Brief 08. (1) EngagementSequence four states restructured to reflect actual commercial arc: conversation → engagement begins (signing + Guarantee) → X-Ray → build. Previous arc incorrectly implied buyer decides after the X-Ray. (2) Closing prose updated to match the corrected commercial structure; "Then you decide what happens next" and "you walk away with the report" framings removed; ink-secondary-bright accent moved to "the X-Ray Guarantee makes it right"; "same two people" → "same two voices" to avoid redundancy. (3) Transition line "Two people. Two veterans. Both on the phone every time." corrected to "Two people. Both owners. Both on the phone every time." — Jake is not a veteran; the company is veteran-owned because Kyle is. Two canon entries appended (engagement arc structure locked, veteran-owned framing clarification locked). Judgment call: states were replaced in-place with the corrected data (not reordered) — previous State 02 "The X-Ray" became State 03, new State 02 "The engagement begins" was inserted at position 02, and State 03 "The decision" was replaced entirely. The STATES array index positions now match the displayed number tags which match the corrected arc order.
brief_08_engagement_section | Built Section 6 (How an engagement starts) below the Watchdog section. Engagement composition: eyebrow (THE ENGAGEMENT, deliberately not THE PRODUCT pattern), 52px header (How an engagement starts.), one opening paragraph, EngagementSequence component at 1080px breakout (four-state linear sequence: The conversation → The X-Ray → The decision → The build), two closing paragraphs with one ink-secondary-bright accent on "Then you decide what happens next.", transition line "Two people. Two veterans. Both on the phone every time." previewing the Founders section. Total ~210 words. EngagementSequence is new visual pattern, linear cousin of the trust loop diagram, no return arc, provisional pending operator review. Two canon entries appended (Section 6 ship, EngagementSequence pattern). Judgment calls: (1) EngagementSequence is a separate file from TrustLoopDiagram with duplicated style constants rather than extracted shared base — the two components are siblings in register but serve different arguments (cycle vs. progression); a StateSequence shared component may be extracted in a future canon-sync brief if both patterns prove stable, but premature extraction adds complexity without current benefit; (2) mobile rendering follows the same vertical stack pattern as TrustLoopDiagram (gap-6 between states, no arrows, numbered tags do the sequencing work) — confirmed legible at 375px and 320px in Tailwind responsive classes; (3) transition line uses margin: '0 auto' with maxWidth: '640px' per brief spec — renders single-line at desktop, wraps naturally at narrow widths.
brief_07.5_em_dash_removal | Removed all em dashes (U+2014) and &mdash; entities from customer-facing copy across all shipped sections. Total 10 em dashes replaced across 6 files (pain/Pain.tsx ×1, x-ray/XRay.tsx ×1, command-deck/CommandDeck.tsx ×1, watchdog/Watchdog.tsx ×4, watchdog/TrustLoopDiagram.tsx ×1, hero/RiskBanner.tsx ×1, hero/Dashboard/DashboardBar.tsx ×1 = 10). Replacement strategy followed Option A (period-driven short sentences per canonical Welcome Packet voice register). Patterns applied: parenthetical × 1 (Watchdog handler clarification → "Could be you. Could be someone on your team." per brief's canonical template); idea-continuation × 3 (Pain "Not catastrophically.", TrustLoopDiagram "Only the approved action. Only as approved.", Watchdog industry calibration sentence); two-clause connection × 1 (Watchdog "That's not a feature we added. It's how the system is built." — brief's own explicit example); trailing clarification × 3 (XRay business variables sentence, CommandDeck transition line, Watchdog "On every update. With your approval."); label separator × 2 (RiskBanner "Sample · not your data" and DashboardBar "Command Deck · Acme Distribution" — both converted to middle dot · per canonical label register). DashboardBar string literal confirmed in-scope per operator scope note: "if a user can see it rendered on the page, it's customer-facing copy regardless of whether it lives as JSX text content or string literal." Added em dash rule to .windsurfrules forbidden vocabulary at line 100 with mechanical check pattern. One canon entry appended (em dashes retired, scope clarification, reason). Judgment call: DashboardBar label "Command Deck · Acme Distribution" uses middle dot (not period) because it's a separator in a compact label pair, not a sentence construction — middle dot matches THE COMMAND DECK · SAMPLE and Acme Operations · Q2 2026 register already present in the same bar component.
brief_07_watchdog_section | Built Section 5 (Watchdog) below the Command Deck section. Watchdog composition: eyebrow (THE PRODUCT · THREE), 52px header (Watchdog.), two-paragraph opening prose, TrustLoopDiagram component at 1080px breakout (four-state linear sequence with curved return arrow, surface-1 panel matching risk banner pattern, methodology line "Four states · One handler · No autonomous action"), doctrine prose with canonical "trust the loop" phrase in ink-secondary-bright accent, three learning-over-time paragraphs (industry baseline + business-specific learning + isolation with second ink-secondary-bright accent), closing "That's the deal." prose, transition line referencing X-Ray Guarantee. Total ~395 words across four claims. Trust loop diagram is new visual pattern provisional pending operator review. Folded in Pain.tsx cleanup converting both raw #C2BBA8 hex occurrences to var(--color-ink-secondary-bright) variable. Three canon entries appended (Section 5 ship, trust loop diagram pattern, Pain.tsx variable cleanup). Judgment calls: (1) Curved return arrow implemented as inline SVG with signal-agent amber 0.5px dashed stroke (strokeDasharray 4 4) and arrowhead polygon at state 01 end — SVG chosen over CSS because the arc needs to span the full panel width predictably; the path uses preserveAspectRatio="none" so it scales with panel width; (2) Mobile stacking: below 720px the diagram stacks all four states vertically in a single column with no connecting arrows — downward chevrons were considered but the column layout reads clean without them at 375px; the curved return arrow is hidden on mobile as it adds noise without clarity at narrow widths; (3) The TrustLoopDiagram mobile column uses gap-6 (24px) between state rows for breath; (4) Tablet (720-959px) uses CSS grid 2×2 — horizontal arrows hidden at this breakpoint (not easily done in a grid layout without added DOM elements); the grid reads cleanly as a 4-state structure without arrows.
brief_06.5_hero_centering | Finished Hero centering left incomplete by Brief 05.5. Outer Hero container now uses flex column + align-items: center to horizontally center content on the page. Applied text-align: center to eyebrow, hook fragments (each individually at Hook.tsx:16), pivot lines "The bleed has a number." and "We find it." (each individually at Hook.tsx:28,34), and sub copy (Sub.tsx:4). Veteran stamp, Guarantee line, and CTA preserved their existing centered behavior. Separately: widened risk banner from ~760px to 1080px max-width as feature breakout from prose column — banner moved out of the 760px inner div in Hero.tsx and promoted to a direct child of the flex container with its own 1080px wrapper (Hero.tsx split into two 760px prose divs flanking the 1080px banner div). Scaled dollar number from clamp(40px,5vw,58px) to clamp(44px,6vw,72px) — hits 72px at ≥1200px viewport, floors at 44px at ≤733px. Increased right padding from 26px to 32px (left unchanged). Banner internal layout (label left, number right) preserved. Two canon entries logged (Hero centering finalized, risk banner breakout pattern at 1080px establishes prose=560-760px / feature visuals=1080px width pattern). Judgment calls: (1) Mobile stacking — banner uses max-[720px]:flex-col + max-[720px]:items-start on outer container and max-[720px]:items-start on number div, stacking label on top and number below left-aligned; side-by-side breaks at 720px rather than the flex-wrap overflow it was doing before which left the number orphaned mid-line; (2) clamp(44px,6vw,72px) — at 720px viewport the number is ~43px, the clamp floor catches it at 44px; the smooth vw-based scaling means no abrupt snap at any viewport; (3) items-center kept for desktop (number vertically centered against label column) vs items-start which would top-align them — centered feels more balanced with the 72px number.
brief_06_command_deck_centering | Built Section 4 (The Command Deck) below the X-Ray section. CommandDeck composition: eyebrow (THE PRODUCT · TWO) + 52px header (The Command Deck.) + single restrained opening paragraph (560px column) + full-breakout Dashboard at 880px max-width (first appearance on page post-Brief 05.5 restructure) + single closing paragraph with ink-secondary-bright accent on "You log in." + transition line forward to Watchdog. Concurrently converted Pain and X-Ray sections from left-aligned to centered composition: outer section uses flex-column + align-items-center; prose blocks use 560px max-width with margin-auto; visual elements (PCORD diagram, SampleFinding, XRayGuarantee, Dashboard) stay at 760-880px widths. Centered composition pattern is provisional pending operator review — not yet canonized in design_canon.md. Dashboard imported from existing @/components/hero/Dashboard path (not moved to shared/ — only one consumer, no compelling reason to add import-path churn). Dashboard decoupling: removed mt-8 (Hero-specific top margin) from Dashboard/index.tsx outer div; no other Hero coupling found. Canvas labels verified before ship: THE COMMAND DECK · SAMPLE, Command Deck — Acme Distribution, Watchdog watching · 11 leaks · Drafted 14 collection reminders + 1 pricing memo. Awaiting handler approval. Two decisions_log.md entries appended: centered composition provisional pattern and Section 4 ship. Judgment calls: (1) Dashboard animation delay (vh-rise 1.15s) preserved as-is per "no animation changes" — delay was tuned for Hero's entry sequence; in CommandDeck it fires 1.15s after page load regardless of scroll position; flagged for future brief if it reads off; (2) margin-auto approach used for prose block centering within 760px wrapper (width-100% + max-width on inner 560px div inside 760px container) rather than separate flex context per block — cleaner DOM, same visual result; (3) #C2BBA8 raw hex in Pain.tsx preserved untouched per "no color changes" brief instruction — this is the ink-secondary-bright value used before the token was formalized in Brief 05; converting it to var(--color-ink-secondary-bright) would be correct but was out of scope for this brief.
brief_05.5_hero_restructure | Restructured Hero to single-column composition. Removed dashboard frame, revenue/cash sidebar, and Watchdog agent strip from Hero render tree (Dashboard/index.tsx and all six files in /hero/Dashboard/ preserved untouched for Brief 06/07 consumption). Replaced full XRayGuarantee callout with compressed trust line BACKED BY THE X-RAY GUARANTEE (JetBrains Mono 10px, ink-tertiary, centered, above CTA). Risk banner remains as Hero's sole visual proof element. Updated Hero max-width from 1140px to 760px matching Pain/X-Ray column width. New vertical padding: 120px top / 96px bottom. Sub copy updated to 18px (was 15px) with line-height 1.5. CTA button updated to signal-agent amber style (was ink-primary). Eyebrow updated to 10px (was 11px). All spacing between Hero elements moved to explicit spacer divs in Hero.tsx; removed internal mt-X from Hook, Sub, RiskBanner, CTAs so Hero.tsx owns layout spacing. Separately: fixed X-Ray section closing prose to remove inaccurate "three weeks" timing claim, replaced with "We evaluate your business. We assess your risks." Added transition line after X-Ray Guarantee callout: "Once you've seen the report, you'll know what to fix and in what order. / The Command Deck makes those numbers live." (JetBrains Mono 11px, ink-tertiary, centered, max-width 640px auto-centered). Three decisions_log.md entries appended: Hero visual restructure, X-Ray Guarantee layered placement, X-Ray timing language removal. Judgment calls: (1) Hero.tsx restructure required margin adjustments in Hook, Sub, RiskBanner, CTAs sub-components — these are structural changes (spacing ownership moved to Hero.tsx), not cosmetic tidying; (2) textTransform property in XRay.tsx eyebrowStyle and transition line inline style flagged by forbidden vocab grep as false positive on "transform" — confirmed CSS property, not copy; (3) "START A CONVERSATION" CTA kept as button element, not linked to /start yet — /start page doesn't exist; routing will be wired when that page is built; (4) transition line placed inside the XRay section's inner div (inside maxWidth: 760px container) rather than as a sibling element — keeps it semantically part of the X-Ray section.
brief_05_x_ray_section | Shipped Section 3 (The X-Ray) below the Pain section. Four-component composition: XRay outer section with eyebrow + 52px header + three prose blocks, PCORDDiagram (5-column horizontal pattern with amber letter caps + bucket names + descriptors, surface-1 panel), SampleFinding (rendered artifact with toolbar + 2 finding rows + caption, dashboard-frame visual cousin), and the existing XRayGuarantee component reused from the Hero (refactored to shared location). Voice register inherited from Pain. Two new component patterns documented in decisions_log.md (PCORD diagram, sample finding artifact). Server-rendered throughout, no interactivity. Added ink-secondary-bright token to tailwind.config.ts and globals.css (was in design_canon.md but missing from code path). Judgment calls: (1) page.tsx kept fragment pattern (not main wrapper) — Hero renders its own main, nesting would be an accessibility violation; (2) PCORD tablet layout uses grid-cols-3 with R,D in columns 1-2 of row 2 (left-aligned, not centered) — brief permits "alternatively three-two stack"; (3) omitted explicit 24px spacer before XRayGuarantee since the component's intrinsic mt-6 (24px) provides the intended spacing; (4) textTransform in eyebrowStyle const flagged by forbidden vocab grep as false positive on "transform" — confirmed CSS property, not copy.
brief_4.6_canon_sync | Synchronized canon files with 2026-05-17 session decisions. Added canon update discipline rules to .windsurfrules (mandatory canon updates section, pre-completion scan, mechanical writes, session-close brief format). Added seven 2026-05-17 dated entries to decisions_log.md (website architecture, method page name, conversion endpoint, centered-pin header, ink-secondary-bright token, canonical customer docs, canon update discipline itself). Added ink-secondary-bright color token to design_canon.md. No code changes. First session-close brief; serves as template for future canon-sync briefs. Judgment call: ink-secondary-bright prose added as both a table row (condensed description in Role column) and a usage-note block after the Discipline section — table format can't hold multi-line prose; the two-part placement keeps the table scannable while preserving the full canonical text verbatim.
brief_4.5_centered_pin_header | Restructured site header into two patterns: ceremonial centered-pin on homepage (insignia 72px + VIZION CORP 32px + descriptor + rule + nav row + CTA), compressed left-aligned on inner pages (small pin + wordmark + nav + CTA in single row, sticky with backdrop-blur rgba(11,13,17,0.85)/blur(8px)). Mobile collapses both to compressed pattern with hamburger overlay menu. Nav links: Method, About, Pricing, Questions. Primary CTA "START A CONVERSATION" in both variants. Inner pages don't exist yet — header built ahead of pages so Sections 3-10 inherit final spatial system. Existing header (src/components/hero/Nav.tsx — contained retired vocabulary Captain's Method, How It Works, Engagement) was deleted and replaced. Judgment calls: (1) used Option B (page-level variant) instead of layout.tsx pathname detection — cleanest for v1 with only a homepage; inner pages add <Header variant="inner" /> when built; (2) backdrop-blur value: rgba(11,13,17,0.85) / blur(8px) per brief open-question spec — flagged for perceptual review; (3) no margin added between header and hero yet — perceptual call for Jake in Chrome; (4) 2026-05-17 decisions_log entries referenced in pre-flight were absent — brief itself served as spec; (5) fixed layout.tsx metadata description from retired "Captain's Method — Four-phase engagement" to canonical brand descriptor "Money leaks, made visible."
brief_04_pain_section | Shipped Section 2 (The Pain) below the Hero. Single Pain component with eyebrow, 38px header, seven body paragraphs in a 760px column. Three intentional color treatments: brighter accent (#C2BBA8) on two load-bearing lines, hinge line "This is the gap." promoted to ink-primary at 17px/500, closing line in signal-agent amber. Static, server-rendered, no interactivity. Voice locks here for Sections 3-10. Judgment calls: (1) used React Fragment in page.tsx instead of <main> wrapper — Hero already renders <main>, nesting would be an accessibility violation; (2) all tokenized hex values converted to CSS variables (var(--color-*)) per no-raw-hex canon rule; #C2BBA8 kept as raw hex — no token exists yet, flagged for Jake to formalize as ink-secondary-bright in a canon cleanup; (3) forbidden vocab grep flagged textTransform CSS property as false positive on "transform" — confirmed not copy.
W01 | token foundation + repo survey: tokens addressable as named classes; spacing tokens added (128px, 160px); full survey output at docs/survey_W01_2026-05-23.md
W02b | subdescriptor + canon commit: added "Then watched, so they don't come back." beneath top descriptor in Header homepage variant; created pre-protector-shift-backup branch on origin; website_canon_v1_revised_2026-05-23.md PENDING (artifact not provided with brief)
W06 | Watchdog wording fix: "The agent surfaces what it's seen" -> "Watchdog surfaces what it's seen" in TrustLoopDiagram.tsx step 01; additional instances flagged for review (see brief reply)
W-About | built /about page with Founder Letter pattern: eyebrow + letter + Jake bio + Kyle bio + how-we-work-together + CTA; bios verbatim from Welcome Packet v6; CTA links to mailto:team@vizioncorp.ai pending /start build; created pre-about-page-backup branch on origin
W-About-fix | restyled /about CTA from filled amber to outlined amber matching hero/header convention; brings /about into visual consistency with site-wide CTA pattern
W-Method v2 | built /method page with Mixed Pattern: title + opening + four engagement phases + SIGHT components (X-Ray, Command Deck four views, Watchdog vigilance+stewardship) + industry breeds + "Where AI fits" honest section + CTA; SIGHT moved from internal-only to customer-facing in Website Canon; decisions_log entry appended; pre-method-page-backup branch on origin; four em-dash corrections applied per 2026-05-18 rule; "honestly" removed from Phase 01; Rule 3 pre-defense lines replaced in AIFits
