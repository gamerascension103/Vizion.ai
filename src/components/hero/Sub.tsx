export function Sub() {
  return (
    <p
      className="mt-5 font-sans text-[15px] font-normal tracking-[-0.005em] text-ink-secondary max-w-[540px]"
      style={{ animation: 'vh-rise 0.6s cubic-bezier(0.16,1,0.3,1) 1.15s both' }}
    >
      The X-Ray finds the leaks. The Command Deck visualizes them. Watchdog watches them. Handler approves every action.
    </p>
  )
}
