export function Sub() {
  return (
    <p
      className="mt-5 font-sans text-[15px] font-normal tracking-[-0.005em] text-ink-secondary max-w-[540px]"
      style={{ animation: 'vh-rise 0.6s cubic-bezier(0.16,1,0.3,1) 1.15s both' }}
    >
      A risk evaluation that identifies what&apos;s actually costing you the most, a dashboard built
      for your business, and an AI agent on watch. Operator approves every action.
    </p>
  )
}
