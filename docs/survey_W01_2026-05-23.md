# Survey W01 — 2026-05-23
## Repo state for Website Build · downstream brief drafting

---

## A1. Brief numbering

**Next available brief number:** The existing numeric series tops out at `brief_08.5`. Next numeric slot is `09` (or `09.5` for a half-step). The W-series (`W01`) is a new sub-series introduced by this brief for the Website Build phase.

**Recommendation:** W01 is appropriate as the opening entry for the Website Build phase. The W-prefix distinguishes Website Build briefs from the prior numeric series. If operator prefers a single flat numbering system, the next slot is `09`.

**Last five shipped briefs (BUILD_LOG.md, lines 1–5 = most recent to oldest):**

| Line | Brief identifier | Summary |
|---|---|---|
| 1 | `website_vocab_cleanup` | Replaced 4 deprecated terms across site |
| 2 | `brief_00_project_setup` | Scaffolded Next.js + TS + Tailwind, canon docs, tokens |
| 3 | `brief_01_hero_section` | Shipped hero: nav, veteran stamp, hook, risk banner, CTAs |
| 4 | `brief_02_insignia` | Shipped Insignia component, BrandLockup, favicon |
| 5 | `brief_03_hero_rebuild` | Migrated hero to locked customer-facing product naming |

Note: BUILD_LOG.md stores entries with newest at line 1 (prepended). The series spans `brief_00` through `brief_08.5` plus `website_vocab_cleanup`, totaling 16 shipped briefs.

---

## A2. Tailwind config state

**Path:** `tailwind.config.ts` (repo root)

**`theme.extend.colors` — current entries (13 tokens, all flat keys):**

| Token name | Hex |
|---|---|
| `ground` | `#0B0D11` |
| `surface-1` | `#0E1116` |
| `surface-2` | `#11141A` |
| `rule-primary` | `#22272F` |
| `rule-inner` | `#1A1E25` |
| `ink-primary` | `#EDE6D5` |
| `ink-secondary` | `#A39C8C` |
| `ink-secondary-bright` | `#C2BBA8` |
| `ink-tertiary` | `#8A8F99` |
| `ink-quaternary` | `#5E6168` |
| `signal-agent` | `#F2B33D` |
| `signal-leak` | `#E36842` |
| `signal-leak-border` | `#4A2218` |

**Structure note:** All tokens use flat hyphenated keys (e.g., `"ink-primary"`). The brief's requested structure uses nested objects (e.g., `ink: { primary: '...' }`). In Tailwind 3, both structures produce identical utility class names (`text-ink-primary`, `bg-ground`, etc.). No functional difference. Brief W01 treats this as **State 1** — tokens already addressable.

**Extra tokens not in brief spec:** `surface-1` and `surface-2` (used throughout components for panel backgrounds). These are valid and in-use; no action needed.

**`theme.extend.fontFamily` — current entries:**
- `sans`: `['var(--font-inter-tight)', '-apple-system', 'system-ui', 'sans-serif']`
- `mono`: `['var(--font-jetbrains-mono)', 'Menlo', 'monospace']`

Exact match to brief spec. **State 1.**

**`theme.extend.spacing` — prior state:** Absent. Brief W01 adds `{ '32': '128px', '40': '160px' }`. **Done.**

**Live-site palette token status:** All canonical color tokens (warm-parchment ink hierarchy, deep slate ground, amber/amber-red signals) exist as named Tailwind tokens. No raw hex in tailwind.config.ts.

**Token addressability verification (pre-build):**
- `bg-ground` → `#0B0D11` ✓
- `text-ink-primary` → `#EDE6D5` ✓
- `text-signal-agent` → `#F2B33D` ✓
- `border-rule-primary` → `#22272F` ✓

---

## A3. globals.css state

**Path:** `src/app/globals.css`

**`:root` block — CSS custom properties (13 entries):**

```css
:root {
  --color-ground: theme('colors.ground');
  --color-surface-1: theme('colors.surface-1');
  --color-surface-2: theme('colors.surface-2');
  --color-rule-primary: theme('colors.rule-primary');
  --color-rule-inner: theme('colors.rule-inner');
  --color-ink-primary: theme('colors.ink-primary');
  --color-ink-secondary: theme('colors.ink-secondary');
  --color-ink-secondary-bright: theme('colors.ink-secondary-bright');
  --color-ink-tertiary: theme('colors.ink-tertiary');
  --color-ink-quaternary: theme('colors.ink-quaternary');
  --color-signal-agent: theme('colors.signal-agent');
  --color-signal-leak: theme('colors.signal-leak');
  --color-signal-leak-border: theme('colors.signal-leak-border');
}
```

**Token ownership pattern:** Tailwind owns the tokens (defined in `tailwind.config.ts`). `globals.css` re-exposes them as CSS custom properties via `theme()` for use in inline styles. This is the correct split — no migration needed.

**Flag (doc discrepancy, no code change):** `design_canon.md` line 121 references `var(--ink-secondary-bright)` (missing `color-` prefix). All component code consistently uses `var(--color-ink-secondary-bright)`. The canon doc has a typo. Recommend fixing the doc in a future canon-sync brief.

**Flag (raw hex in SampleFinding.tsx):** `src/components/x-ray/SampleFinding.tsx` uses raw hex values `#22272F`, `#1A1E25`, and `rgba(242, 179, 61, 0.4)` in inline styles. This violates the "color tokens, not raw hex" canon rule. Token-substitution is downstream brief work; noted here for scheduling.

---

## A4. Header component

**File path:** `src/components/header/Header.tsx`
**Component name:** `Header` (named export)
**Imported in:** `src/app/page.tsx` line 1 as `import { Header } from '@/components/header'`, rendered at line 12 as `<Header variant="homepage" />`

**Exact descriptor JSX (homepage variant, lines 39–41):**
```tsx
<span className="font-mono font-medium text-[9px] tracking-[0.24em] text-signal-agent">
  Money leaks, made visible
</span>
```

**Terminal period:** No terminal period. Descriptor renders verbatim: `Money leaks, made visible` — matches expected live-site behavior.

**Page tree composition:** `src/app/page.tsx` → `<Header variant="homepage" />` (first child in the fragment, above `<Hero />`).

---

## A5. Font loading

- **Inter Tight:** Loaded via `next/font/google` in `src/app/layout.tsx` (lines 5–10). Weights: 400, 500, 600, 700. CSS variable: `--font-inter-tight`. display: swap.
- **JetBrains Mono:** Loaded via `next/font/google` in `src/app/layout.tsx` (lines 12–17). Weights: 400, 500. CSS variable: `--font-jetbrains-mono`. display: swap.
- **Cormorant Garamond:** NOT loaded anywhere in the codebase. Confirmed absent from `layout.tsx`, `globals.css`, and all components. ✓

---

## A6. Type scale on live homepage

Surveyed from: `Eyebrow.tsx`, `Hook.tsx`, `Sub.tsx`, `Hero.tsx`, `Pain.tsx`, `XRay.tsx`, `Watchdog.tsx`, `Engagement.tsx`, `EngagementSequence.tsx`, `TrustLoopDiagram.tsx`, `SampleFinding.tsx`, `LeaksPanel.tsx`, `Header.tsx`.

| Role | Face | Size (desktop) | Size (mobile) | Weight | Tracking |
|---|---|---|---|---|---|
| Hero eyebrow | JetBrains Mono | 10px | 10px | 500 | 0.24em |
| Hero hook fragments | Inter Tight | 30px | 26px (max-sm) | 400 | −0.022em |
| Hero hook pivot | Inter Tight | 44px | 30px (max-sm) | 600 | −0.032em |
| Hero sub copy | Inter Tight | 18px | 16px (≤720px) | 400 | −0.005em |
| Header wordmark (homepage) | Inter Tight | 32px | 14px | 600 | 0.34em |
| Header descriptor | JetBrains Mono | 9px | — | 500 | 0.24em |
| Section eyebrow labels | JetBrains Mono | 10px | 10px | 500 | 0.24em |
| Pain H2 | Inter Tight | 38px | 28px (≤720px) | 600 | −0.028em |
| X-Ray H2 | Inter Tight | 52px | 36px (≤720px) | 600 | −0.03em |
| Watchdog H2 | Inter Tight | 52px | 28px (≤720px) | 600 | −0.03em |
| Engagement H2 | Inter Tight | 52px | 28px (≤720px) | 600 | −0.03em |
| Body paragraphs (all sections) | Inter Tight | text-base (~16px) | 15px (≤720px) | 400 | −0.003em |
| Pain hinge line | Inter Tight | 17px | 17px | 500 | −0.01em |
| Card titles (sequence/loop diagrams) | Inter Tight | 18px | 18px | 500 | — |
| Card sub-descriptions | Inter Tight | 13px | 13px | 400 | — |
| Dashboard leak name | Inter Tight | 13px | 13px | 400 | −0.005em |
| Dashboard leak detail | Inter Tight | 11px | 11px | 400 | — |
| Dashboard monthly value | JetBrains Mono | 12px | — | 500 | tabular-nums |
| Dashboard annual value | JetBrains Mono | 13px | — | 500 | tabular-nums |
| SampleFinding title | Inter Tight | 18px | 16px (≤719px) | 500 | 0 |
| SampleFinding cost value | JetBrains Mono | 22px | 18px (≤719px) | 500 | tabular-nums |
| SampleFinding row detail | Inter Tight | 14px | 13px (≤719px) | 400 | — |
| Transition lines | JetBrains Mono | 11px | 10px (≤720px) | 500 | 0.18em |
| Button / CTA | JetBrains Mono | 11px | 11px | 500 | 0.14em |

**H2 inconsistency to flag for canon:** Pain section uses 38px H2 (weight 600, tracking −0.028em). All other section H2s use 52px (weight 600, tracking −0.03em). This may be intentional (Pain is a shorter, more intimate section) but the tracking also differs slightly. Worth noting for the type-scale lockdown in Website Canon v1.

---

## A7. Watchdog section live wording

**File:** `src/components/watchdog/TrustLoopDiagram.tsx`

**Step 01 verbatim (line 2):**
```
label: 'Watchdog proposes'
sub:   'The agent surfaces what it's seen'
```

**Verdict:** The wording still reads **"The agent surfaces what it's seen."** It has NOT been changed. **Brief W06's primary copy fix is still needed.** W06 should replace "The agent surfaces what it's seen" with whatever the canon-correct phrasing is (TBD per W06 brief).

---

## Part B — Token foundation assessment

| Category | Status | Action taken |
|---|---|---|
| Color tokens | **State 1** — all 13 canonical tokens named in Tailwind config | None |
| Font families | **State 1** — exact match to brief spec | None |
| Spacing `'32'`/`'40'` | **Missing** — `theme.extend.spacing` absent | Added `{ '32': '128px', '40': '160px' }` |
| globals.css | **Correct pattern** — Tailwind owns tokens, globals.css re-exposes as CSS vars | None |
| Cormorant Garamond | **Absent** — confirmed not loaded | None |

**Note on spacing:** Tailwind 3's default spacing scale already includes keys `32` (= `8rem`) and `40` (= `10rem`), which equal 128px and 160px at 16px root font-size. The `theme.extend` entries added by this brief pin them as explicit `px` values, overriding the default `rem` values for those two keys only. Functionally equivalent at current root font-size.

---

## Additional flags for downstream briefs

1. **Raw hex in `SampleFinding.tsx`** — `#22272F`, `#1A1E25`, `rgba(242,179,61,0.4)` used in inline styles. Canon violation. Schedule a token-cleanup brief.
2. **doc discrepancy in `design_canon.md` line 121** — `var(--ink-secondary-bright)` should be `var(--color-ink-secondary-bright)`. Fix in next canon-sync brief.
3. **`website_canon_v1_revised_2026-05-23.md` not present in `docs/`** — referenced as canon source in Brief W01 but not committed to the repo. Operator should add it to `docs/` before or alongside Brief W02b.
4. **Pain H2 size (38px) vs all other H2s (52px)** — may be intentional; flag for type-scale decision in Website Canon update.
